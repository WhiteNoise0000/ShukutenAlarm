<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- DataStore (`datastore` ドメイン) をバックアップ対象に含める -->
    <include domain="datastore" path="."/>
    <!-- SharedPreferences (`sharedpref` ドメイン) は除外する -->
    <exclude domain="sharedpref" path="."/>
</full-backup-content>
